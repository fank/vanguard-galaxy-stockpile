bin/
obj/
*.user
.vs/
.idea/
*.suo
dist/
# Assembly-CSharp.dll is a publicized stub committed at VGStockpile/lib/ so
# the GitHub Actions release workflow can compile against it without the
# proprietary game runtime.
